def test__valid_input():
    """GIVEN the variables as input to the get_jupyter_link
    WHEN they look as expected
    THEN get_jupyter_link returns a link containing the variable values
    """
    assert 1 == 1
